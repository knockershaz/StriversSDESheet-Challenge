#include <bits/stdc++.h> 
void sort012(int *a, int n)
{
   int l=0,m=0,h=n;
   while(m<h){
      if(a[m]==0){
         swap(a[m],a[l]);
         l++;
         m++;
      }
      else if(a[m]==2){
      --h;
         swap(a[m],a[h]);
         
      }
      else{
         m++;
      }
   }
}
